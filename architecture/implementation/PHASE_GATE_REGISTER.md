# Phase Gate Register

| Gate | Required Before | Status | Evidence |
|---|---|---|---|
| Phase 1 input validation | Stage A code generation | PASSED | Previous Stage A evidence plus `PHASE1_DIRECTORY_RECONCILIATION.md`. |
| Complete handoff ingestion | Stage 0B checkpoint | PASSED_WITH_DISCREPANCIES | All required path, checksum and validation outputs created; hotfix/owner count drift recorded. |
| Native PostgreSQL runtime | Stage 0B checkpoint | PASSED | Stage 0C verified PostgreSQL 16.14, database `ois_nextgen`, role `ois_nextgen`, schema `public`; `pnpm db:migrate` passed. |
| Prisma client generation | Stage 0B checkpoint | PASSED | Stage 0C `$env:CI='true'; pnpm db:generate` passed and generated Prisma Client v6.19.3. |
| Seed idempotency | Stage 0B checkpoint | PASSED | Stage 0C fixed seed no-op writes and verified two reruns with identical overall table fingerprint `4a83d1852d3eca5ea2970f7204825b1d4719a344fe27155ec272d404019054f7`. |
| Core API root contract | Stage 0B checkpoint | PASSED | Core API `/`, `/docs`, `/health`, `127.0.0.1` and `::1` returned HTTP 200 while canonical `pnpm dev` was running. |
| Local HTTP runtime | Stage 0B checkpoint | PASSED | `pnpm dev` serves OIS Console on 3000, PITS Shell on 3001, Core API root on 4000, `/docs` and `/health` simultaneously. Console/PITS scripts bind explicitly to `127.0.0.1`. |
| Cloud CI readiness | Stage 0D checkpoint | PASSED_WITH_MANUAL_GITHUB_SETUP | GitHub Actions CI and release preflight workflows added with PostgreSQL service, migration, seed, lint, typecheck, unit, e2e smoke and build gates. Local Stage 0D gates pass; first GitHub run and branch protection remain manual. |
| GitHub CI activation | Stage 0E checkpoint | PASSED | PR #1 final-head CI run `28738991764` completed successfully on commit `622d42d42572e44ace34795f860231e5027feb55`; artifact `stage-0d-evidence` exists and contains Console, PITS and Core API docs screenshots. |
| Codex Cloud and GitHub test bootstrap | Stage 0G checkpoint | CODEX_CLOUD_APP_BOOT_VERIFIED | Stage 0G verified lint, typecheck, tests, recursive builds and a mock-safe split-app local cloud-test boot. Core API, OIS Console and PITS Shell returned HTTP 200 on ports 4000, 3000 and 3001. Processes were stopped after evidence capture. No Abacus deploy, migration, production resource or real secret was used. |
| Abacus staging runtime handoff | Stage 0H checkpoint | HANDOFF_READY_ABACUS_INPUTS_BLOCKED | Stage 0H converts Stage 0G boot evidence into a no-deploy Abacus staging handoff package. Split-app commands, ports, healthchecks, mock-safe env names, DB-backed exclusions, rollback/stop procedure and expected POC evidence are documented. Abacus owner inputs remain missing, so no Abacus deploy or runtime POC was executed. |
| Abacus staging inputs runtime config | Stage 0I checkpoint | ABACUS_INPUTS_STILL_BLOCKED | Stage 0I attempted discovery/config-only inspection. Repo commands and expected env names remain documented, but no Abacus connector, CLI, authenticated UI, screenshots or owner-filled checklist were available. Project ID, SuperComputer/cloud ID, public URL, GitHub/source state, service IDs, env/secrets mechanism, live mock config and port/proxy behavior remain unknown or blocked. No deploy or runtime POC was executed. |
| Abacus source bootstrap evidence | Stage 0I-R1 checkpoint | ABACUS_SOURCE_CONNECTED_RUNTIME_STILL_BLOCKED | Owner-assisted Abacus SuperComputer source verification confirms GitHub connection, cloned repo path `/home/ubuntu/ois-nextgen`, remote `origin`, branch `stage-0b-complete-handoff-ingestion`, commit `64486c1ebf9d5bc96cadc8220d1616dea9ccbf70`, and clean/up-to-date working tree. No package install, build, runtime, migration, secret printing, deploy or production resource use occurred. Runtime port/proxy behavior, env/secrets injection and public URL mapping remain unknown. |
| Abacus Core API only POC | Stage 0J checkpoint | ABACUS_CORE_API_LOCAL_BOOT_VERIFIED_PUBLIC_MAPPING_BLOCKED | Owner/Abacus Agent evidence confirms Abacus VM install, lint, typecheck, tests, recursive build and mock-safe Core API local boot at `127.0.0.1:4000`. Core API `/health` and `/` returned HTTP 200. Console and PITS were not started, DB-backed endpoints were not called, no migrations or `prisma db push` ran, no production credentials were used, and the process was stopped. Public `/health` on `https://ois-nextgen.abacusai.cloud` returned HTTP 404 because routing/deployment was not configured. |
| Abacus preview public routing POC | Stage 0K checkpoint | ABACUS_CORE_API_PREVIEW_PUBLIC_HEALTH_VERIFIED | Owner/Abacus Agent evidence confirms Core API bound to `0.0.0.0:4000` and Abacus VM preview proxy route `https://7a162f29d-4000.na116.preview.abacusai.app/health` returned HTTP 200 with the Core API health payload. Hosted-app custom domain `https://ois-nextgen.abacusai.cloud/health` still returned HTTP 404 because no hosted-app deployment or Always-On app was performed. No DB/storage-backed endpoints, migrations, `prisma db push`, production credentials, secrets or `.env` commit were used. |
| Abacus hosted-app custom domain POC | Stage 0L checkpoint | ABACUS_HOSTED_APP_CUSTOM_DOMAIN_DEPLOY_BLOCKED_FROM_VM | Owner/Abacus Agent evidence confirms VM preview routing still works and `https://ois-nextgen.abacusai.cloud/` returns edge placeholder body `READY`, but `https://ois-nextgen.abacusai.cloud/health` remains HTTP 404 because no hosted-app backend is mapped. No VM-side `abacus`/`abacusai` deploy CLI or generic Node/Fastify hosted-app SDK path was found. Hosted-app service registration requires Abacus platform/console owner assistance. |
| Abacus resource boundary SuperComputer contract | Stage 0N checkpoint | ABACUS_RESOURCE_BOUNDARY_SUPERCOMPUTER_DEPLOYMENT_CONTRACT_READY | Stage 0N records the audited architecture: Codex/GitHub is the engineering/test plane, Abacus SuperComputer is the staging VM/App Shell plane, `https://ois-nextgen.abacusai.cloud` is an Abacus-managed public domain, VM preview proxy is temporary, external `dmp247.com` domains are optional publication layers, and App Shell resources are isolated by DB name and S3 numeric prefix. OIS NextGen staging may use only `default` DB and `59543/`; Phase 1 and Emerald/BQL DBs/prefixes must not be touched. |
| Abacus managed domain Core API health | Stage 0O checkpoint | ABACUS_MANAGED_DOMAIN_CORE_API_HEALTH_VERIFIED | Owner/Abacus Agent evidence confirms Core API-only staging health on the Abacus-managed public domain. `@ois/core-api` runs under systemd on the SuperComputer, nginx proxies `/health` to `127.0.0.1:4000`, and `https://ois-nextgen.abacusai.cloud/health` returns HTTP/2 200 with the Core API health payload. Console, PITS and worker were not started; DB-backed functionality is not enabled; no migrations, `prisma db push`, production credentials, real storage values or real OpenRouter key were used. |
| Default DB Prisma baseline | Stage 0P checkpoint | DEFAULT_DB_PRISMA_BASELINE_APPLIED | Owner/Abacus Agent evidence confirms readiness label `DEFAULT_DB_PRISMA_READINESS_CONFIRMED` and execution label `DEFAULT_DB_MIGRATION_APPLIED_SUCCESS`. `pnpm db:migrate` mapped to `prisma migrate deploy`, applied `202607040001_platform_kernel` to the `default` DB only, and `prisma migrate status` reported the database schema is up to date. Public `/platform/overview` returned HTTP 200 with zero counts from live Prisma reads. No `prisma db push`, `prisma migrate dev`, production resources, row data inspection, seed, `/auth/demo-login` or write endpoint was used. |
| Platform Kernel seed | Stage 0Q checkpoint | PLATFORM_KERNEL_SEED_APPLIED | Owner/Abacus Agent evidence confirms readiness label `PLATFORM_KERNEL_SEED_SCRIPT_READY` and execution label `PLATFORM_KERNEL_SEED_APPLIED`. `pnpm db:seed` populated 66 `DEMO DATA - NOT PRODUCTION` records across 18 application tables in the `default` DB. Public `/platform/overview` returned HTTP 200 with seeded Platform Kernel counts, while `/health` remained HTTP 200. `PLATFORM_KERNEL` remains `IN_PROGRESS` by API-controlled logic, expected. No `prisma db push`, `prisma migrate dev`, secrets, legacy resources, write HTTP endpoints, `/auth/demo-login`, nginx or systemd changes were used. |
| Platform Kernel gate smoke stabilization | Stage 0R-A checkpoint | PLATFORM_KERNEL_TEST_COVERAGE_REQUIRED | Code inspection confirms `/platform/overview` is read-only and uses eight Prisma `count()` queries. `phaseGates.PLATFORM_KERNEL` is a hardcoded API response literal `IN_PROGRESS`, not count-driven, config-driven, feature-flag-driven or manually DB-controlled. The behavior matches Stage 0Q expectations, but focused tests for overview count mapping, gate behavior, no-DB health and legacy/prod reference guards are still required before gate advancement. |
| Platform Kernel test coverage | Stage 0R-B checkpoint | PLATFORM_KERNEL_TEST_COVERAGE_ADDED | Local-only tests now cover no-DB `/health`, `/platform/overview` read-only Prisma count mapping, current phase gate statuses, DB-unavailable HTTP 500 behavior and static legacy/prod resource guards. `buildCoreApi()` accepts an optional Prisma client for mocked tests while default runtime behavior remains unchanged. No deploy, runtime modification, migration, seed, `prisma db push`, live endpoint probe or production/legacy resource touch occurred. |
| Abacus runtime sync no regression | Stage 0R-C checkpoint | ABACUS_RUNTIME_SYNCED_NO_BEHAVIOR_REGRESSION | Owner/Abacus Agent evidence confirms the Abacus runtime fast-forwarded from `cc7ed28704c9e804385f6d2a4c21e8d887a775e3` to integration commit `e862b98ea601fa6ab8be6b78fd3ebbde5e66c66d`, validation passed, Core API restarted and remained active, `/health` and `/platform/overview` returned HTTP 200 locally and publicly, seeded counts remained stable, and `PLATFORM_KERNEL` remained `IN_PROGRESS`. No migrations, `prisma db push`, seed, schema change, nginx/systemd unit change, write endpoint, `/auth/demo-login`, secret print or legacy resource touch occurred. |
| Safe SSH operations | Stage 0R-D checkpoint | SAFE_SSH_OPERATIONS_READY | Reusable `ops/abacus` scripts and `docs/deployment/ABACUS_SSH_OPERATIONS.md` now cover owner-run SSH status, active endpoint checks, safe Core API restart, runtime sync and guarded Stage 0O rollback. Defaults are read-only except explicitly named restart/sync actions; rollback requires `--confirm-rollback`; scripts avoid secret printing, migrations, seed, `prisma db push`, write endpoints and default legacy probes. |
| Abacus SSH relay diagnostic | Stage 0R-E checkpoint | ABACUS_SSH_RELAY_BLOCKED_WEB_TERMINAL_FALLBACK_READY | Owner external SSH attempts to `ois-nextgen.ssh4.abacusai.cloud` on port `22469` failed from Wi-Fi and 5G; `Test-NetConnection` returned `TcpTestSucceeded False` and `ssh -vvv` timed out before authentication. Abacus internal diagnostics confirmed healthy in-VM `sshd`, valid `authorized_keys`, working local VM SSH TCP path and no in-VM tunnel agent, so the blocker is Abacus platform edge/relay routing. Web Terminal plus `ops/abacus/*.sh` is the fallback operation path until the relay is fixed. |
| Ops restart grace window | Stage 0R-F checkpoint | OPS_RESTART_GRACE_WINDOW_ADDED | Owner Web Terminal evidence showed immediate post-restart local connection failures and public HTTP 502 responses followed by a passing later `status.sh`, confirming a Core API warm-up race rather than a behavior regression. `safe-restart-core-api.sh` now waits up to 30 seconds, retrying every 2 seconds, for local `/health`, public `/health` and local/public `/platform/overview`; transient failures are labeled `WARMING_UP` until `RESTART_VERIFICATION_TIMEOUT`. |
| Ops script unbound variable fix | Stage 0R-G checkpoint | OPS_SCRIPT_UNBOUND_VARIABLE_FIX_READY | After Stage 0R-F was pulled to Abacus integration commit `124741498e1557e660aa1f960f9f5c8e1c3e55c9`, `status.sh` crashed with `lib-core-api-checks.sh: line 110: body: unbound variable` while the systemd service remained active/running. The bug was shell helper variable shadowing under `set -u`, not Core API runtime failure. Stage 0R-G initializes response variables, removes shadowing in `http_get_body`, reports empty-body/HTTP 502/timeout cases cleanly and adds a no-network helper self-test. |
| Two UI shell demo | Stage 0S-A checkpoint | TWO_UI_SHELL_DEMO_READY_FOR_ABACUS_PREVIEW | OIS Console and PITS Shell now have minimal root demo/status pages ready for Abacus preview. Both are server-rendered Next pages, use `CORE_API_URL` or `NEXT_PUBLIC_CORE_API_URL` with default `https://ois-nextgen.abacusai.cloud`, call Core API `/health` and `/platform/overview`, show seeded Platform Kernel counts and keep UI DB access behind Core API only. No Abacus deploy, migration, seed, `prisma db push`, production/legacy resource touch or runtime modification occurred. |
| Two UI shell preview ops | Stage 0S-B checkpoint | TWO_UI_SHELL_PREVIEW_OPS_READY | Safe Abacus Web Terminal scripts now support temporary OIS Console and PITS Shell preview operations on ports 3000 and 3001. The scripts use `CORE_API_URL=https://ois-nextgen.abacusai.cloud`, unset UI `DATABASE_URL`, avoid Core API/nginx/DB changes, infer Abacus preview proxy URLs from `PREVIEW_URL` or `APP_ORIGIN`, and verify HTTP 200 pages with product markers and seeded Core API counts. |
| Abacus App Shell deployment contract | Stage 0T-A checkpoint | ABACUS_APP_SHELL_DEPLOYMENT_CONTRACT_READY | Owner evidence shows Apps Management Console is the source of truth for App Shell deployment. OIS Console and PITS Shell must each become their own Abacus App Shell with separate managed deployment URL, database/storage/version/domain lifecycle and env config that calls shared Core API only. SuperComputer preview URLs on ports 3000/3001 are deprecated for App Shell proof; Connected Services are connectors; Abacus CLI is blocked until API metering is enabled. |
| UI demo test harness | Stage 0T-B checkpoint | UI_DEMO_TEST_HARNESS_READY | Local/Codex Vitest coverage now renders the OIS Console and PITS Shell demo pages with mocked Core API `/health` and `/platform/overview` responses, verifies shell names, product codes, Core API URL, demo banner, health OK state and seeded counts, and adds a static guard blocking direct DB and legacy/production references in UI packages. No deploy, Abacus runtime change, migration, seed, `prisma db push`, production credential or legacy resource touch occurred. |
| OIS Console App Shell deploy | Stage 0T-C checkpoint | OIS_CONSOLE_APP_SHELL_DEPLOYED | Owner/Abacus App Shell evidence confirms `OIS NextGen Console Demo` deployed as a Next.js App Shell at `https://161acd4ff8.na116.preview.abacusai.app`, using `apps/ois-console`, `@ois/ois-console`, port 3000 and shared Core API `https://ois-nextgen.abacusai.cloud`. Screenshot evidence shows `OIS_CONSOLE`, Core API health OK, demo banner and canonical seeded counts. PITS was not deployed, Core API was not modified, no migrations/seed/`prisma db push` ran, no write endpoints or `/auth/demo-login` were called, no production credentials or `dmp247.com` custom domain were used, and OIS Phase 1 remained untouched. |
| PITS Shell upload bundle | Stage 0T-D-R1 checkpoint | PITS_SHELL_UPLOAD_BUNDLE_READY | Abacus reported the PITS Shell App Shell environment cannot clone the external GitHub repo directly. Stage 0T-D-R1 keeps source-of-truth repo provenance by preparing a direct upload ZIP with `apps/pits-shell`, `packages/shared-ui`, root package/lock/workspace metadata and `tsconfig.base.json`, while excluding env files, generated output, `.git`, secrets, runtime files and unrelated apps/domains/Prisma assets. No deploy, runtime mutation, migration, seed, `prisma db push`, production credential or legacy resource touch occurred. |
| PITS Shell App Shell deploy from bundle | Stage 0T-D-R2 checkpoint | PITS_SHELL_APP_SHELL_DEPLOYED_FROM_BUNDLE | Owner/Abacus evidence confirms `pits-shell-abacus-upload-bundle.zip` deployed successfully from `/home/ubuntu/pits_shell_bundle` as a PITS Shell App Shell at `https://113d93f4db-3001.na116.preview.abacusai.app`. The bundle preserved `apps/pits-shell`, `packages/shared-ui` and root workspace files; `pnpm install --frozen-lockfile`, `pnpm --filter @ois/pits-shell build` and `pnpm --filter @ois/pits-shell start` passed; the page returned HTTP 200, displayed `PITS_SHELL`, showed Core API `https://ois-nextgen.abacusai.cloud`, health OK, demo banner and seeded counts. No `DATABASE_URL`, direct DB access, migration, seed, `prisma db push`, production credential, write endpoint, `/auth/demo-login` or legacy resource touch occurred. |
| Two App Shell verification | Stage 0T-D-R2 checkpoint | TWO_APP_SHELL_VERIFICATION_BLOCKED_BY_OIS_CONSOLE_PREVIEW_404 | Stage 0T-D-R2 combined verification is blocked only because the previously deployed OIS Console preview `https://161acd4ff8.na116.preview.abacusai.app` returned HTTP 404. This is recorded as `OIS_CONSOLE_APP_SHELL_PREVIEW_UNAVAILABLE_404` and must not be treated as a PITS Shell deployment failure. Restore or redeploy OIS Console before the two-App-Shell verification gate. |
| OIS Console upload bundle | Stage 0T-E-A-R1 checkpoint | OIS_CONSOLE_UPLOAD_BUNDLE_READY | Stage 0T-E-A-R1 prepares a direct upload bundle for restoring/redeploying OIS Console because the previous OIS Console preview remains `OIS_CONSOLE_APP_SHELL_PREVIEW_UNAVAILABLE_404`. The bundle packages `apps/ois-console`, `packages/shared-ui`, root package/lock/workspace metadata and `tsconfig.base.json`, while excluding env files, generated output, `.git`, secrets, runtime files and unrelated apps/domains/Prisma assets. No deploy, runtime mutation, migration, seed, `prisma db push`, production credential or legacy resource touch occurred. |
| Product subdomain routing demo ops | Stage 0U-A checkpoint | PRODUCT_SUBDOMAIN_ROUTING_DEMO_OPS_READY | Stage 0U-A adds safe owner-run nginx scripts for SuperComputer host-based routing of `ois-ng.dmp247.com` to OIS Console on port 3000 and `pits-ng.dmp247.com` to PITS Shell on port 3001. The scripts create/remove only `/etc/nginx/conf.d/ois-nextgen-product-subdomains.conf`, keep Core API on `ois-nextgen.abacusai.cloud`, document that DNS CNAME maps hostnames only, and provide local Host-header plus optional public DNS/TLS verification labels. No deploy, DNS change, Abacus runtime mutation, migration, seed, `prisma db push`, production credential or legacy resource touch occurred from this workspace. |
| Product subdomain runtime verification | Stage 0U-A runtime checkpoint | CUSTOM_SUBDOMAIN_TLS_BLOCKED | Owner/Abacus runtime evidence confirms `ois-ng.dmp247.com` and `pits-ng.dmp247.com` CNAME records propagated to `ois-nextgen.abacusai.cloud`. Local nginx Host-header checks passed for OIS Console and PITS Shell with HTTP 200, product codes, Core API URL and seeded counts. Public HTTPS checks for both roots and `/dashboard`/`/projects` failed with SSL handshake failure, and public HTTP roots returned HTTP 409. The blocker is Abacus edge/TLS custom-host registration, not OIS/PITS app behavior, Core API behavior or local nginx routing. |
| Cloudflare Tunnel custom subdomain plan | Stage 0V-A checkpoint | CLOUDFLARE_TUNNEL_CUSTOM_SUBDOMAIN_PLAN_READY | Stage 0V-A documents the owner-selected Cloudflare Tunnel path after Abacus confirmed custom hostnames are not supported for SuperComputer and are supported only for managed App Shells. Planned routing is `ois-ng.dmp247.com` -> tunnel -> `127.0.0.1:3000`, `pits-ng.dmp247.com` -> tunnel -> `127.0.0.1:3001`, optional later `api-ng.dmp247.com` -> tunnel -> `127.0.0.1:4000`. This stage is documentation only: no `cloudflared` install, DNS change, deploy, migration, seed, `prisma db push`, production credential or Cloudflare token commit occurred. |
| Cloudflare Tunnel public subdomains | Stage 0V-B/C checkpoint | CLOUDFLARE_TUNNEL_PRODUCT_SUBDOMAINS_VERIFIED | Owner runtime evidence confirms Cloudflare Tunnel `ois-nextgen-abacus` is healthy with 1 active replica, 2 routes and `cloudflared` version `2026.6.1`. `https://ois-ng.dmp247.com` opens OIS Console, `/dashboard` opens OIS Platform Overview, `https://pits-ng.dmp247.com` opens PITS Shell and `/projects` opens PITS Project Selector. Both shells show product codes, shared Core API `https://ois-nextgen.abacusai.cloud`, healthy Core API HTTP 200 and seeded counts through Core API only. No tunnel token or connector credential was documented or committed. |
| Public staging runtime hardening | Stage 0W-A checkpoint | PUBLIC_STAGING_RUNTIME_HARDENING_READY | Stage 0W-A adds safe owner-run ops scripts for durable OIS/PITS public staging systemd services, public staging restart/status and public endpoint checks. UI services use ports 3000/3001 and Core API `https://ois-nextgen.abacusai.cloud` without `DATABASE_URL`; Core API service and cloudflared token/credentials are not modified. `runtime-sync.sh` defaults to Core API-only restart and can restart Core API plus UI services only with `PUBLIC_STAGING_RESTART_SCOPE=all`; cloudflared restart requires explicit `RESTART_CLOUDFLARED=true`. |
| Public staging runtime operation | Stage 0W-B runtime checkpoint | PUBLIC_STAGING_RUNTIME_OPERATIONAL | Owner runtime execution of Stage 0W-A passed after stopping legacy temporary UI demo processes and restarting durable OIS/PITS systemd services. Core API, OIS Console, PITS Shell and cloudflared were active. OIS/PITS Cloudflare Tunnel public roots and secondary routes passed, and `check-public-staging-endpoints.sh` passed. |
| Public staging runtime hotfix | Stage 0W-B hotfix checkpoint | PUBLIC_STAGING_RUNTIME_SECRET_SAFE_HOTFIX_READY | Stage 0W-B makes public staging status token-safe by replacing full `systemctl status cloudflared` output with `systemctl is-active` plus selected `systemctl show` properties only. It also updates public staging restart to stop legacy temporary UI demo processes before OIS/PITS systemd restart and to print safe port diagnostics for 3000/3001 without process command lines or secrets. |
| Product shell navigation baseline | Stage 1A checkpoint | PRODUCT_SHELL_NAVIGATION_BASELINE_READY | OIS Console and PITS Shell now have first real product shell navigation baselines. OIS routes `/`, `/dashboard`, `/products`, `/workspaces` and `/runtime` share Core API-sourced Platform Kernel and runtime cards. PITS routes `/`, `/projects` and `/runtime` share project selector, overview and runtime cards. UI shells use Core API only, avoid `DATABASE_URL`, and public endpoint checks now include the expanded route set. |
| Product shell route 404 hotfix | Stage 1A-R1 checkpoint | PRODUCT_SHELL_ROUTE_404_HOTFIX_READY | Stage 1A owner runtime verification found local/public HTTP 404s on new OIS `/products`, `/workspaces`, `/runtime` and PITS `/runtime` routes while root/dashboard/projects and Core API stayed healthy. Stage 1A-R1 confirms the source pages and local production route manifests exist, then adds generated `.next` cleanup plus a production route manifest/server-entry guard before Abacus UI service restarts. No Cloudflare/DNS, migration, seed, `prisma db push`, credential or legacy-resource change occurred. |
| UI orphan port cleanup | Stage 1A-R2 checkpoint | UI_ORPHAN_PORT_CLEANUP_READY | Owner/manual Abacus evidence after Stage 1A-R1 confirms `ORPHAN_UI_PORT_PROCESS_BLOCKED_SYSTEMD_RESTART`: route artifacts were present, but orphan Next.js processes held ports `3000` and `3001`. Manual `systemctl stop`, `fuser -k`, `reset-failed` and `start` restored OIS/PITS systemd services and all Stage 1A local/public routes passed. Stage 1A-R2 adds the same safe cleanup to public staging restart ops, targeting only `3000/tcp` and `3001/tcp` before local/public verification. |
| Read-only Platform Registry API data binding | Stage 1B checkpoint | READONLY_PLATFORM_REGISTRY_API_DATA_BINDING_READY | Stage 1B adds read-only Core API routes `/platform/products`, `/platform/workspaces`, `/platform/projects`, `/platform/modules`, `/platform/installations` and `/platform/registry` using Prisma read queries only. OIS Console and PITS Shell bind product/workspace/project/module/installation surfaces to Core API registry data, keep UI DB access behind Core API, and extend local tests plus public staging ops checks. No Cloudflare/DNS, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, write endpoint or legacy-resource change occurred. |
| Product Registry detail cross-linking | Stage 1C checkpoint | PRODUCT_REGISTRY_DETAIL_CROSS_LINKING_READY | Stage 1C adds read-only Core API detail routes for products, product code lookup, workspaces, projects, modules and installations, plus OIS Console detail pages and a PITS Shell project detail page. Shared UI helpers build staging-only cross-links between OIS and PITS product shells. Public staging ops discover seeded IDs, verify detail APIs, controlled 404s and dynamic detail routes. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, write endpoint or legacy-resource change occurred. |
| Product Registry detail UI marker hotfix | Stage 1C-R1 checkpoint | PRODUCT_REGISTRY_DETAIL_UI_MARKER_HOTFIX_READY | Owner Abacus runtime evidence showed Core API detail endpoints, route manifests, systemd services, cloudflared and root/list/runtime pages passed, but OIS/PITS detail route verification failed because expected markers were missing from HTML responses. Stage 1C-R1 adds explicit server-rendered visible `data-detail-source` markers for product, workspace, module, installation and project detail pages. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, write endpoint or legacy-resource change occurred. |
| Registry runtime health owner UAT | Stage 1D checkpoint | REGISTRY_RUNTIME_HEALTH_OWNER_UAT_READY | Stage 1D adds read-only Core API `/platform/registry/health`, owner-visible OIS/PITS registry runtime health panels and ops checks for configured, linked, reachable, missing URL, not applicable, degraded and unavailable states. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, write endpoint, `/auth/demo-login` change or legacy-resource touch occurred. |
| Registry governance installation lifecycle readiness | Stage 1E checkpoint | REGISTRY_GOVERNANCE_INSTALLATION_LIFECYCLE_READY | Stage 1E adds read-only Core API `/platform/registry/readiness`, owner-visible OIS/PITS governance/readiness panels and ops checks for ready, incomplete, blocked, not applicable and unknown states. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, Prisma UI import, write endpoint, `/auth/demo-login` change or legacy-resource touch occurred. |
| Owner registry cockpit visual UAT | Stage 1F checkpoint | OWNER_REGISTRY_COCKPIT_VISUAL_UAT_READY | Stage 1F adds owner-visible OIS/PITS cockpit and visual UAT navigation surfaces that summarize existing registry counts, health, readiness, missing link/runtime URL guard status, forbidden link guard status and cross-product links. It adds no endpoint and remains read-only. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, Prisma UI import, write endpoint, `/auth/demo-login` change or legacy-resource touch occurred. |
| OIS Console root cockpit marker hotfix | Stage 1F-R1 checkpoint | OWNER_REGISTRY_COCKPIT_ROOT_MARKER_HOTFIX_READY | Stage 1F-R1 renders deterministic server-side `Ready to operate` text on the OIS Console root page so existing root cockpit ops checks pass without weakening checks or forcing the real cockpit status. No deploy, Cloudflare/DNS change, migration, seed, `prisma db push`, credential, UI `DATABASE_URL`, Prisma UI import, write endpoint, `/auth/demo-login` change or legacy-resource touch occurred. |
| Published endpoint registry | All future endpoint-affecting stages | ESTABLISHED | `docs/deployment/PUBLISHED_ENDPOINT_REGISTRY.md` records local/loopback, Codex Cloud, Abacus VM local, Abacus preview proxy, Abacus-managed public staging, legacy production/do-not-touch and future planned endpoints. Future stage reports must include a Published Endpoint Delta with added, changed, unchanged, deprecated/stopped, do-not-touch and current test checklist entries. |
| Abacus readiness foundation | Stage 0D checkpoint | MANUAL_SETUP_REQUIRED | Abacus staging and production readiness docs plus deployment manifest template added. No production deploy or credentials used. |
| Abacus staging runtime POC | Stage 0F checkpoint | OWNER_REGISTRY_COCKPIT_ROOT_MARKER_HOTFIX_READY | Split app staging remains the product-safe topology. Stage 0O verified Core API `/health` on `https://ois-nextgen.abacusai.cloud/health`; Stage 0P migrated the `default` DB and verified DB-backed read-only `/platform/overview`; Stage 0Q applied demo/staging Platform Kernel seed data and verified seeded `/platform/overview` counts; Stage 0R-A confirmed gate logic; Stage 0R-B added focused local-only tests; Stage 0R-C synced Abacus runtime with no behavior regression; Stage 0R-D added safe SSH/Web Terminal operations; Stage 0R-E confirmed direct external SSH is blocked by Abacus relay routing; Stage 0R-F added restart grace-window retries; Stage 0R-G fixed the ops helper unbound-variable crash; Stage 0S-A prepared separate OIS Console and PITS Shell demos; Stage 0S-B added safe preview ops scripts; Stage 0T-A corrected the canonical UI path to Apps Management Console App Shell deployments; Stage 0T-B added local/Codex UI demo test harness coverage; Stage 0T-C deployed OIS Console as its own App Shell; Stage 0T-D-R1 prepared the PITS Shell direct upload bundle after GitHub clone was blocked; Stage 0T-D-R2 deployed PITS Shell from that bundle; Stage 0T-E-A-R1 prepared an OIS Console direct upload bundle for restoration; Stage 0U-A verified local SuperComputer product subdomain routing and DNS propagation, but Abacus custom hostname/TLS blocked public direct-CNAME routing; Stage 0V-A planned Cloudflare Tunnel; Stage 0V-B/C verifies Cloudflare Tunnel public HTTPS for OIS/PITS product staging subdomains; Stage 0W-A prepares durable systemd-backed public staging operations; Stage 0W-B records operational runtime evidence and patches token-safe status/restart cleanup behavior; Stage 1A prepares product-shell navigation baselines; Stage 1A-R1 prepares the route 404 hotfix with deterministic UI build cleanup and manifest checks; Stage 1A-R2 makes orphan UI port cleanup permanent for public staging restarts; Stage 1B adds read-only Platform Registry API routes and binds OIS/PITS shells to Core API registry data; Stage 1C adds read-only registry detail APIs and OIS/PITS cross-product links; Stage 1C-R1 fixes the detail UI marker contract for runtime verification; Stage 1D adds owner-verifiable registry runtime health surfaces; Stage 1E adds owner-verifiable registry governance/readiness surfaces; Stage 1F adds owner-verifiable cockpit and visual UAT navigation surfaces; Stage 1F-R1 fixes the OIS root `Ready to operate` marker contract. Worker, `/auth/demo-login`, write endpoints and production/legacy custom domains remain out of scope. |
| Platform kernel bootstrap | PITS vertical slices | PASSED | Kernel schema, seed, hierarchy, permission/capability, audit, idempotency and optimistic concurrency tests pass. |
| Phase 2 business rules | PITS, Knowledge, Learning, Wisdom, Intelligence behavior | READY_WITH_CONDITIONS | Verified behavior imported; 18 coverage gaps, 10 unverified critical APIs and owner decisions remain open. |
| Phase 2 API/UI contracts | Legacy compatibility and UI route mapping | READY_WITH_CONDITIONS | 273 route and 90 UI-page baselines preserved; unverified APIs are not authoritative contracts. |
| Phase 3 starter data | Full demo data and migration validation | READY_WITH_CONDITIONS | 220 fixtures imported as handoff reference; Stage 0B seed remains platform-kernel demo data only. |
| Phase 3 regression suite | Regression certification | READY_WITH_CONDITIONS | Regression queue created from 23 layers, 81 blueprint test cases and 135 minimum tests. |
| PITS Field Report vertical slice | Next implementation stage | NOT_STARTED | Explicitly deferred until after Stage 0B checkpoint. |
